name: tfsec
permissions: write-all
on:
  pull_request:
  workflow_dispatch:
jobs:
  tfsec:
    uses: clouddrove/github-shared-workflows/.github/workflows/security-tfsec.yml@v2
    secrets: inherit
    with:
      working_directory: '.'
